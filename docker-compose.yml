version: "3.9"

services:

  postgres:
    image: postgres:17-alpine
    container_name: billing-postgres

    restart: unless-stopped

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: billing_user_db

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

    networks:
      - billing-network

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  user-service:
    container_name: user-service

    build:
      context: .
      dockerfile: user-service/Dockerfile

    restart: unless-stopped

    depends_on:
      postgres:
        condition: service_healthy

    env_file:
      - ./user-service/.env.docker

    ports:
      - "3002:3002"

    networks:
      - billing-network

  route-service:
    container_name: route-service

    build:
      context: .
      dockerfile: route-service/Dockerfile

    restart: unless-stopped

    depends_on:
      postgres:
        condition: service_healthy

    env_file:
      - ./route-service/.env.docker

    ports:
      - "3007:3007"

    networks:
      - billing-network

  trip-service:
    container_name: trip-service

    build:
      context: .
      dockerfile: trip-service/Dockerfile

    restart: unless-stopped

    depends_on:
      postgres:
        condition: service_healthy

    env_file:
      - ./trip-service/.env.docker

    ports:
      - "3008:3008"

    networks:
      - billing-network

  booking-service:
    container_name: booking-service

    build:
      context: .
      dockerfile: booking-service/Dockerfile

    restart: unless-stopped

    depends_on:
      postgres:
        condition: service_healthy

    env_file:
      - ./booking-service/.env.docker

    ports:
      - "3009:3009"

    networks:
      - billing-network

  api-gateway:
    container_name: api-gateway

    build:
      context: .
      dockerfile: api-gateway/Dockerfile

    restart: unless-stopped

    depends_on:
      - user-service
      - route-service
      - trip-service
      - booking-service

    env_file:
      - ./api-gateway/.env.docker

    ports:
      - "3000:3000"

    networks:
      - billing-network

networks:
  billing-network:

volumes:
  postgres-data: